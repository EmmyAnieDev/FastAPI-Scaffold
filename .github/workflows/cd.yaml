name: CD - Deploy on Push

on:
  push:
    branches: [main, dev]

jobs:
  deploy:
    name: 🚀 Continuous Deployment
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: 🚀 Echo CD placeholder
        run: echo "cd ..."
